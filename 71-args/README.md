# Aula 71 - *args Para quantidade de Argumentos não nomeados 📋

Nesta aula, compreendi o uso do `*args` para funções que precisam receber uma quantidade variável de argumentos.

### 📝 Principais aprendizados:

* `Flexibilidade:` O `*args` permite que a função aceite múltiplos argumentos posicionais sem a necessidade de definir ou nomear cada um deles individualmente na criação da função.
* `Empacotamento:` Internamente, o Python "empacota" todos os valores enviados em uma tupla, o que permite iterar sobre eles ou realizar operações como soma e contagem.
* `Sintaxe:` O nome `args` é uma convenção da comunidade, mas o que define o comportamento é o asterisco (`*`).

### ✅ Itens para o seu Checklist:
* [x] Criar funções que aceitam múltiplos argumentos com `*args`.
* [x] Utilizar o laço `for` para processar os elementos de uma tupla `args`.
* [x] Desempacotamento: Usar o `*` para enviar os itens de uma lista/tupla individualmente para uma função.
