# Aula 65 - "def" Introdução às funções 📋

Nesta aula, compreendi que a função é utilizada para replicar uma determinada ação ao longo do código, eliminando a necessidade de repetir o mesmo trecho várias vezes.

### 📘 conceitos:

* **Parâmetros e Argumentos:** As funções podem receber valores de entrada para processar dados dinamicamente.
* **Parâmetros:** São as "variáveis que definimos (detro dos parênteses do `def`).
* **Argumentos:** São os valores que enviamos para a função o momento em que a chamamos (a invocação).
* **Retorno (return):** Elas podem devolver um valor específico após a execução.

---
### 📝 Definição Rápidas:

* Parâmetro: É nome definido na criação da função (Ex: `def soma(a, b):` -> `a` e `b` são parâmetro).
* Argumento: É valor passado na execução (Ex: `soma(10, 5)` -> `10` e `5` são argumentos). 